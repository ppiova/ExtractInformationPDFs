{
  "name": "ExtractInformationPDFs",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/azure-cli:1": {}
  },
  "postCreateCommand": "pip install --upgrade pip && pip install -r app/requirements.txt && pip install pytest && ([ ! -f .env ] && cp app/.env.example .env && echo 'Archivo .env creado desde .env.example. Por favor, completa los valores de configuración de Azure.' || echo '.env ya existe.')",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.testing.pytestEnabled": true,
        "python.testing.pytestArgs": [
          "tests"
        ],
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": false,
        "python.formatting.provider": "none"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-azureresourcegroups"
      ]
    }
  },
  "remoteUser": "vscode"
}
